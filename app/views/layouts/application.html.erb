<!DOCTYPE html>
<html>
  <head>
    <title>Evolve</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>

    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload', defer: true %>
<link rel="icon" type="image/png" href="https://www.pngarts.com/files/1/Letter-E-PNG-Picture.png" />
  </head>

  <body>
    <%= render 'shared/flashes' %>
    <%= render 'shared/navbar' %>
    <% unless current_page?(root_path) || current_page?('/users/sign_in') || current_page?('/users/sign_up')%>
      <div class="grid">
        <%= render "shared/menu" %>
        <div class="body-content">
          <%= yield %>
        </div>
      </div>
    <% else %>
      <%= yield %>
    <% end %>

    <%= render 'shared/footer' %>
  </body>
</html>
