<div class="dabig-div">
  <div class="confetti-position">
    <%= image_tag "smaller-confetti.png", alt: "Victoire!" %>
  </div>
  <div class="congrat-text">
    <p>Vous avez <span class="test-score"><%= @quizz_score.number_correct_answer %></span> bonnes réponses.</p>
    <p>Félicitations, vous avez débloqué la leçon suivante !</p>
  </div>
  <div class="btn-div">
    <% if @quiz == @program.lessons.last.quiz %>
      <%= link_to "Passer au test final", final_test_path(@final_test), class: "btn_round4"  %>
    <% else %>
    <%= link_to "Leçons", program_path(@program), class: "btn_round4"  %>
    <% end %>
  </div>
</div>
